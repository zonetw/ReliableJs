var Conference = Conference || {};

Conference.checkInRecorder = function(){
  "use strict";
  
  return{
    recordCheckIn: function(attendee){
      // Code that records the check-in with an external system
    }
  };
};