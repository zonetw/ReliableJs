var Conference = Conference || {};

Conference.attendeeWebApi = function(){
  'use strict';
  
  return {
    // Post the attendee
    post: function post(attendee) {
      // Code that posts the attendee to the server
    },
    
    // Return a Promise for all attendees.
    getAll: function getAll() {
      // Code that return a promise that returns all the attendees
    }
  };
};

