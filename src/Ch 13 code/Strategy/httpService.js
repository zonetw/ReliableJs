var Conference = Conference || {};

Conference.httpService = function(){
  'use strict';
  
  return {
    post : function post(url, data){
      // Posts data to url, and returns a promise
      // that resoves when the request is complete.
    }
  };
};