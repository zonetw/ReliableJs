var Conference = Conference || {};
'use strict';

Conference.outgoingLinkClickRecorder = function(){
  return {
    // Records an the details of an outgoing link click
    recordClick: function recordClick(clickDetails){
      // Perform necessary steps to record the click in the conference's
      // database.
    }
  };
};